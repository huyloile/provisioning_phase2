%% Defind name of table
-define(Opt_sachem,option_sachem).
-define(Opt_asm,option_asmetier).

%% Define option record sachem
-record(option_sachem,{key,
		       id_orange,
		       subscription,
		       commercial_name,
		       status,
		       comments,
		       subscribe_parameters,
		       unsubscribe_parameters,
		       reactivation_parameters,
		       incompatible_options,
		       commercial_dates,
		       open_hours,
		       open_days,
		       bank_holidays_closure,
		       statistics_id,
		       subscribe_conditions,
		       unsubscribe_conditions,
		       reactivate_conditions
		      }).

%% Define option record asmetier
-record(option_asmetier,{key,
			 subscribe_SO,
			 terminate_SO,
			 subscription,
			 commercial_name,
			 status,
			 comments,
			 incompatible_options,
			 commercial_dates,
			 open_hours,
			 open_days,
			 bank_holidays_closure,
			 statistics_id,
			 subscribe_conditions,
			 unsubscribe_conditions
			}).

%% Define request param record
-record(request_param,{type_action,
		       top_num,
		       date_deb,
		       heure_deb,
		       date_fin,
		       heure_fin,
		       ptf_num,
		       tcp_num,
		       cout,
		       mnt_initial,
		       rnv_num
	 }).
